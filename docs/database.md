# Database

> **Status: IMPLEMENTED** — Schema, migrations, repositories, and DB instance are in place. Initial migration generated at `apps/api/src/db/migrations/0000_smart_stranger.sql`.

## Driver Strategy

By default, **PGLite** is used — a WebAssembly build of PostgreSQL that runs in-process with no server required, storing data in a local file (`./data/qqcms.db`). All schemas use `pgTable` from `drizzle-orm/pg-core`.

For production, set `DATABASE_URL` to a PostgreSQL connection string — the app will switch to the `postgres` driver automatically, with no schema changes needed.

## ORM

Drizzle ORM with Drizzle Kit for migrations.

```
apps/api/src/db/
├── index.ts          # exports `db` instance (PGLite or PostgreSQL)
├── schema/
│   ├── index.ts
│   ├── pages.ts
│   ├── page-translations.ts
│   ├── navigation-items.ts
│   ├── media.ts
│   ├── languages.ts
│   └── settings.ts
├── repository/
│   ├── pages.ts
│   ├── page-translations.ts
│   ├── navigation-items.ts
│   ├── media.ts
│   ├── languages.ts
│   └── settings.ts
└── migrations/       # generated by drizzle-kit
```

Repositories are imported via `@repository/<name>`. The `db` instance is accessed through `@core/Database` (re-export of `src/db/index.ts`).

## Schema

All primary keys are **UUID v4**. All timestamps are `created_at` / `updated_at` stored as ISO 8601 strings.

### `pages`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `slug` | text | Unique, URL-safe identifier |
| `status` | enum | `draft` \| `published` \| `unpublished`. Defaults to `draft` |
| `has_draft` | boolean | `true` when unpublished/published page has pending draft changes |
| `is_homepage` | boolean | Only one page can be `true` at a time; API enforces this |
| `created_at` | timestamp | |
| `updated_at` | timestamp | |

**Page status state machine:**
- New page starts as `draft`
- `PATCH /pages/:id/status` with `{ status: "published" }` → copies draft content to `published_title`/`published_content` on translations
- `PATCH /pages/:id/status` with `{ status: "unpublished" }` → hides from public, keeps published content
- `DELETE /pages/:id/draft` → discards draft, reverts to last published content
- Every `PUT /pages/:id/translations/:lang` on a published/unpublished page sets `has_draft: true`

### `page_translations`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `page_id` | UUID | FK → `pages.id`, cascade delete |
| `language_code` | text | e.g. `en`, `ru`, `et` |
| `title` | text | Draft title in this language |
| `content` | JSON | Draft TipTap editor output (JSON document) |
| `published_title` | text | Last published title (nullable) |
| `published_content` | JSON | Last published content (nullable) |

Unique constraint: `(page_id, language_code)`.

### `navigation_items`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `label` | JSON | `{ "en": "About", "ru": "О нас" }` |
| `href` | text | Target URL or slug |
| `order` | integer | Display order |
| `parent_id` | UUID | Self-referencing FK (nullable, for nested nav) |

### `media`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `filename` | text | Stored filename on disk (e.g. UUID-based) |
| `original_name` | text | Original filename from upload |
| `mime_type` | text | e.g. `image/jpeg` |
| `size` | integer | File size in bytes |
| `url` | text | Public URL path (e.g. `/uploads/abc.jpg`) |
| `created_at` | timestamp | |

### `languages`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `code` | text | Unique, e.g. `en`, `ru`, `et` |
| `label` | text | Display name, e.g. `English` |
| `is_active` | boolean | Only active languages appear on public client |

### `settings`

| Column | Type | Notes |
|---|---|---|
| `id` | UUID | Primary key |
| `key` | text | Unique setting key |
| `value` | JSON | Arbitrary JSON value |

## Migrations

Managed via Drizzle Kit. Never write manual SQL.

```sh
# generate migration after schema change
bunx drizzle-kit generate

# apply migrations
bunx drizzle-kit migrate
```
