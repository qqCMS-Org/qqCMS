# Internationalization (i18n)

> **Status: NOT IMPLEMENTED** — Language routing and auto-detection logic are not yet built in `apps/web`. Language management API is not yet built in `apps/api`.

## Concepts

- **Site languages** — managed by the admin through the admin panel. Stored in the `languages` table.
- **Admin UI languages** — fixed set: `en` and `ru`. Toggled by the admin user inside the admin panel.

These are two separate i18n concerns.

## URL Structure (Public Client)

All pages are served under a language prefix:

```
/{lang}/{slug}

/en/about
/ru/about
/et/kontakt
```

The `{lang}` segment must be one of the **active** languages configured in the admin panel.

## Root URL Redirect

When a visitor opens `/`, the public client:

1. Reads `Accept-Language` header from the browser
2. Matches it against the list of active languages from the API
3. Redirects to `/{matched-lang}/` (homepage slug)
4. If no match → redirects to the **default language** (first active language)

> **Not implemented** — `apps/web/src/middleware.ts` exists but redirect logic is not yet done.

## Language Switching

The public client header displays a language switcher. It shows **only active languages** fetched from the API at build time.

Clicking a language navigates to the same slug in the selected language:
```
/en/about  →  /ru/about
```

## Admin Panel UI Language

The admin panel UI strings (labels, buttons, messages) are available in `en` and `ru`.

The language is stored in `packages/shared` as i18n resources and accessed via a `useTranslation` utility.

> **Not implemented** — `packages/shared` does not yet contain i18n resources.

## Shared Package i18n

```ts
// packages/shared/src/i18n/en.ts (planned)
export const en = {
  pages: {
    title: "Pages",
    create: "Create page",
  },
  nav: {
    title: "Navigation",
  },
  // ...
}
```

## Language Management API (planned)

| Method | Endpoint | Description |
|---|---|---|
| `GET` | `/languages` | List all languages |
| `POST` | `/languages` | Add a new language |
| `PATCH` | `/languages/:id` | Update label or active status |
| `DELETE` | `/languages/:id` | Remove a language |
